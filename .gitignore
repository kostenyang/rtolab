# Python
.venv/
__pycache__/
*.pyc

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# Ansible
*.retry

# Secrets (本機明文; 也包含 sops 解密後的暫存)
# rtolab 用明文 lab.yaml + 強 gitignore (Windows 環境沒 sops/age, 加密過度工程)
inventory/secrets/lab.yaml
inventory/secrets/*.dec.*
inventory/secrets/*.plain.*
!inventory/secrets/lab.example.yaml
!inventory/secrets/.gitkeep

# Age 私鑰絕對不能進 git
keys.txt
*.age.key
age-key*

# PowerCLI logs
batch-*.csv
*.log

# 編輯器
.DS_Store
.vscode/settings.json
*.swp
*~

# ISO / depot (大檔不入 git)
*.iso
*-depot.zip

# Layer 2 bring-up generated / 暫存 spec (含明文密碼, 絕對不能進 git)
layer2-bringup/generated-bringup.json
layer2-bringup/generated-bringup.json.broken.json
layer2-bringup/smoketest-*.json
